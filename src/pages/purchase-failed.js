import React from "react"

const completePage = () => {
  return (
    <div style={{ display: "grid", placeContent: "center", fontSize: "4rem" }}>
      Sorry the transaction failed
    </div>
  )
}

export default completePage
